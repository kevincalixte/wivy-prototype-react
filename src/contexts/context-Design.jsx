import { createContext } from 'react';

// IMAGES DE PRESENTATION DU DESIGN

export const designImages = [
    'assets/logos/wivy-app-download.png',
];

const ContextDesignImage = createContext();

export default ContextDesignImage;